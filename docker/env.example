# MTS Environment Configuration
# Copy this file to .env and modify values as needed

# Database Configuration
DB_HOST=localhost
DB_PORT=25432
DB_USER=mts
DB_PASSWORD=mts_password_2024
DB_NAME=mts

# pgAdmin Configuration
PGADMIN_DEFAULT_EMAIL=admin@mts.local
PGADMIN_DEFAULT_PASSWORD=admin123

# MTS Backend Configuration
MTS_PORT=28080
MTS_CONFIG_FILE=/app/config.yaml

# Development Configuration
DOCKER_BUILDKIT=1
COMPOSE_DOCKER_CLI_BUILD=1

# Logging
LOG_LEVEL=debug

# JWT Configuration (for future use)
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production

# API Configuration
API_BASE_URL=http://localhost:28080/api/v1

# Docker Configuration
COMPOSE_PROJECT_NAME=mts 